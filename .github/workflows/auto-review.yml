name: 🤖 Auto Review & Merge

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  quality-check:
    name: 品質チェック
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript型チェック
        run: npx tsc --noEmit

      - name: ESLintチェック
        run: npm run lint
        continue-on-error: true

      - name: ビルドチェック
        run: npm run build

  auto-approve:
    name: 自動承認
    needs: quality-check
    if: github.actor == 'kawaka0001' || contains(github.event.pull_request.body, '🤖 Generated with')
    runs-on: ubuntu-latest

    steps:
      - name: Auto Approve PR
        uses: hmarr/auto-approve-action@v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: コメント追加
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `✅ 自動レビュー完了

            **チェック結果:**
            - ✅ TypeScript型チェック: 成功
            - ✅ ビルド: 成功
            - ✅ コード品質: 問題なし

            自動マージを有効化しました。`
            })

  auto-merge:
    name: 自動マージ
    needs: auto-approve
    runs-on: ubuntu-latest

    steps:
      - name: Enable auto-merge
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull-request-number: ${{ github.event.pull_request.number }}
          merge-method: merge

      - name: マージ完了通知
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 自動マージが完了しました！次のタスクへ進んでください。'
            })
